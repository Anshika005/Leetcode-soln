select product_name , sum(unit) as unit from products join orders on products.product_id = orders.product_id where month(order_date)=2 and year(order_date)=2020 group by products.product_id having unit>=100; 
